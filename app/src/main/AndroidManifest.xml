<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.enhyun.enhyuntest">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.EnhyunTest"
        android:usesCleartextTraffic="true">
        <activity android:name=".Receipt" />
        <activity android:name=".Join_terms_AC" />
        <activity android:name=".Login_AC" />
        <activity android:name=".Start_AC">

        </activity>
        <activity android:name=".Store_AC"><intent-filter>
            <action android:name="android.intent.action.MAIN" />

            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter></activity>
        <activity android:name=".Join_AC" />
        <activity android:name=".Menu_G_AC" />
        <activity android:name=".Menu_P_AC" />
        <activity android:name=".MainActivity" />
        <activity android:name=".Payment_AC" />
    </application>

</manifest>